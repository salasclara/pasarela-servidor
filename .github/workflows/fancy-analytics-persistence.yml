name: Fancy Analytics Persistence Validation

on:
  pull_request:
    paths:
      - 'src/services/FancyAnalyticsObservation.js'
      - 'src/services/FancyAnalyticsPersistence.js'
      - 'scripts/validate-fancy-analytics-persistence.js'
      - '.github/workflows/fancy-analytics-persistence.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Syntax check
        run: |
          node --check src/services/FancyAnalyticsPersistence.js
          node --check scripts/validate-fancy-analytics-persistence.js
      - name: Validate isolated persistence
        run: node scripts/validate-fancy-analytics-persistence.js
